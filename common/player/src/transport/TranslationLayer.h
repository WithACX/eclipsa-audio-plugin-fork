#pragma once

/**
 * @brief Translates decoded IAMF audio data into a format suitable for
 * playback. Takes input frames and resamples
 *
 */
class TranslationLayer {
 public:
  TranslationLayer() = default;

  ~TranslationLayer() = default;
};